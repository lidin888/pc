--- SConstruct
+++ SConstruct
@@ -364,6 +364,8 @@ Import('socketmaster', 'msgq')
 messaging = [socketmaster, msgq, 'capnp', 'kj',]
 Export('messaging')
 
+# Build other submodules
+SConscript(['tools/webcam/SConscript'])
 
 # Build other submodules
 SConscript(['panda/SConscript'])
